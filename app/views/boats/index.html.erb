<h1>Boats</h1>
<div class="boat-grid">
  <% @boats.each do |boat| %>
    <div class="boat-card">
      <%= link_to boat do %> <!-- correct ? -->
        <h2><%= boat.name %></h2>
        <p><%= boat.price %></p>
      <% end %>
    </div>
  <% end %>
</div>
